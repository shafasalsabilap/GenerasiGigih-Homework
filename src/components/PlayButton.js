import React from "react";

const PlayButton = () => {
  return (
    <div>
      <button>
        PLAY <i className="fa fa-play fa-lg"></i>
      </button>
    </div>
  );
};

export default PlayButton;
